<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_account"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="co.example.hment.myapplication.Fragments.Account">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="@dimen/padding_space">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/manage_your_accounts"
                android:textStyle="bold"
                android:textSize="@dimen/button_text_size"/>

            <ImageButton
                android:id="@+id/add_account"
                android:layout_width="@dimen/edit_btn_size"
                android:layout_height="@dimen/edit_btn_size"
                android:layout_gravity="center_vertical"
                android:src="@drawable/ic_add_black_18px"
                android:background="?attr/selectableItemBackground"/>

        </LinearLayout>


        <ListView
            android:id="@+id/account_list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:padding="@dimen/padding_space">

        </ListView>

    </LinearLayout>

</FrameLayout>
